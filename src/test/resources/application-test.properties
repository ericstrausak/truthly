# Test Configuration
spring.application.name=truthly-test

# Test MongoDB (wird von Testcontainers überschrieben)
spring.data.mongodb.database=truthly-test

# Logging für Tests
logging.level.ch.zhaw.truthly=DEBUG
logging.level.org.springframework.test=DEBUG
logging.level.org.testcontainers=INFO

# Disable Auth0 for tests
spring.security.oauth2.resourceserver.jwt.issuer-uri=
auth0.domain=test-domain